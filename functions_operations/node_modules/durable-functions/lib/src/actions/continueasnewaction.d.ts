import { ActionType, IAction } from "../classes";
/** @hidden */
export declare class ContinueAsNewAction implements IAction {
    readonly actionType: ActionType;
    readonly input: unknown;
    constructor(input: unknown);
}
