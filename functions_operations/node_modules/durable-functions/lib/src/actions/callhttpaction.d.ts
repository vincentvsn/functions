import { ActionType, DurableHttpRequest, IAction } from "../classes";
/** @hidden */
export declare class CallHttpAction implements IAction {
    readonly httpRequest: DurableHttpRequest;
    readonly actionType: ActionType;
    constructor(httpRequest: DurableHttpRequest);
}
