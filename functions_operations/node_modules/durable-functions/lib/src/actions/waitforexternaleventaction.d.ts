import { ActionType, ExternalEventType, IAction } from "../classes";
/** @hidden */
export declare class WaitForExternalEventAction implements IAction {
    readonly externalEventName: string;
    readonly reason: ExternalEventType;
    readonly actionType: ActionType;
    constructor(externalEventName: string, reason?: ExternalEventType);
}
