import { ActionType, EntityId, IAction } from "../classes";
/** @hidden */
export declare class SignalEntityAction implements IAction {
    readonly operation: string;
    readonly actionType: ActionType;
    readonly instanceId: string;
    readonly input: unknown;
    constructor(entityId: EntityId, operation: string, input?: unknown);
}
