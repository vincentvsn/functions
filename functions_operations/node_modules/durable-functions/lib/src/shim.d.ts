import { IEntityFunctionContext, IOrchestrationFunctionContext } from "./classes";
/**
 * Enables a generator function to act as an orchestrator function.
 *
 * Orchestration context methods can be access
 * @param fn the generator function that should act as an orchestrator
 * @example Initialize an orchestrator
 * ```javascript
 * const df = require("durable-functions");
 *
 * module.exports = df.orchestrator(function*(context) {
 *     // function body
 * });
 * ```
 */
export declare function orchestrator(fn: (context: IOrchestrationFunctionContext) => Generator<unknown, unknown, any>): (context: IOrchestrationFunctionContext) => void;
export declare function entity<T = unknown>(fn: (context: IEntityFunctionContext<T>) => void): (context: IEntityFunctionContext<T>) => void;
