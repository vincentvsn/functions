import { IEntityFunctionContext } from "./classes";
/** @hidden */
export declare class Entity<T> {
    fn: (context: IEntityFunctionContext<T>) => void;
    constructor(fn: (context: IEntityFunctionContext<T>) => void);
    listen(): (context: IEntityFunctionContext<T>) => Promise<void>;
    private handle;
    private getCurrentDurableEntityContext;
    private destructOnExit;
    private getInput;
    private getState;
    private return;
    private setState;
    private signalEntity;
    private computeElapsedMilliseconds;
}
