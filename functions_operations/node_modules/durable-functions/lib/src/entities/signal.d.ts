import { EntityId } from "../classes";
/** @hidden */
export declare class Signal {
    readonly target: EntityId;
    readonly name: string;
    readonly input: string;
    constructor(target: EntityId, name: string, input: string);
}
